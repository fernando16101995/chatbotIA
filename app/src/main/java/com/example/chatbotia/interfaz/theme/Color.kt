package com.example.chatbotia.interfaz.theme

import androidx.compose.ui.graphics.Color

// --- PALETA  ---
val BgDark = Color(0xFF0D0D0D)         // Fondo casi negro
val SurfaceDark = Color(0xFF161616)    // Superficie de tarjetas/inputs
val SurfaceVariant = Color(0xFF1E1E1E) // Variante para contrastes sutiles

val AccentViolet = Color(0xFF7C6FF7)   // Violeta refinado
val AccentBlue = Color(0xFF4F8EF7)     // Azul índigo opcional

val TextPrimary = Color(0xFFF0F0F0)    // Texto casi blanco
val TextSecondary = Color(0xFF888888)  // Texto gris (subtítulos)
val TextTertiary = Color(0xFF555555)   // Texto muy sutil (timestamps)

val BorderSubtle = Color(0xFF262626)   // Bordes casi invisibles
val ErrorRed = Color(0xFFFF5252)

// Mantenemos algunos anteriores por compatibilidad temporal si es necesario
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)
